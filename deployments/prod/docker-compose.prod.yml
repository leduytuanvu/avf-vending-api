# Lean production stack: single Ubuntu 24.04 VPS (target: 2 vCPU / 8 GB RAM).
# Copy .env.production.example -> .env.production and fill secrets + image refs before `docker compose up`.
#
# Image-only deployment (GHCR): there is no `build:` anywhere. The VPS pulls prebuilt images only.
# App binaries (`api`, `worker`, `reconciler`, `mqtt-ingest`) share one image ref:
#   ${IMAGE_REGISTRY}/${APP_IMAGE_REPOSITORY}:${APP_IMAGE_TAG}
# Migrations use the goose image:
#   ${IMAGE_REGISTRY}/${GOOSE_IMAGE_REPOSITORY}:${GOOSE_IMAGE_TAG}
#
# Telemetry / NATS / MQTT safety: `api`, `worker`, `mqtt-ingest`, and `reconciler` load `env_file`
# (PROD_ENV_FILE or `.env.production`) for NATS_URL, APP_ENV, TELEMETRY_*, MQTT_*, METRICS_*, etc.
# Avoid duplicating large `environment:` blocks per service unless you need Compose-time overrides (e.g. MQTT_CLIENT_ID).
#
# Intentionally NOT included: ClickHouse, Temporal, Prometheus, Grafana, Loki, MinIO, Redis
# (Redis is optional for this API — leave unset unless you enable features that require it).
#
# Usage (from this directory):
#   docker compose --env-file .env.production -f docker-compose.prod.yml up -d

name: avf-vending-prod

networks:
  edge:
    driver: bridge
  internal:
    driver: bridge
    internal: true

volumes:
  postgres_data:
  nats_data:
  emqx_data:
  caddy_data:
  caddy_config:

services:
  postgres:
    image: postgres:16-alpine
    container_name: avf-prod-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      # Single app database owned by POSTGRES_USER (avoids brittle init scripts for credentials).
      POSTGRES_DB: avf_vending
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - internal
    healthcheck:
      test: ["CMD-SHELL", 'pg_isready -U "$$POSTGRES_USER" -d avf_vending']
      interval: 10s
      timeout: 5s
      retries: 12
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 768M
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  nats:
    image: nats:2.10-alpine
    container_name: avf-prod-nats
    restart: unless-stopped
    command: ["-js", "-sd", "/data", "-m", "8222"]
    volumes:
      - nats_data:/data
    networks:
      - internal
    # No host ports — JetStream / core NATS reachable only inside `internal` network.
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8222/healthz >/dev/null"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 256M
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "2"

  emqx:
    image: emqx/emqx:5.6.0
    container_name: avf-prod-emqx
    hostname: emqx
    restart: unless-stopped
    environment:
      EMQX_NAME: emqx
      EMQX_HOST: emqx
      EMQX_NODE__NAME: emqx@emqx
      EMQX_CLUSTER__DISCOVERY_STRATEGY: manual
      EMQX_DASHBOARD__DEFAULT_USERNAME: ${EMQX_DASHBOARD_USERNAME:?set EMQX_DASHBOARD_USERNAME in .env.production}
      EMQX_DASHBOARD__DEFAULT_PASSWORD: ${EMQX_DASHBOARD_PASSWORD:?set EMQX_DASHBOARD_PASSWORD in .env.production}
      # Listen on all interfaces inside the container so host port publish 127.0.0.1:18083:18083 works.
      # Host binding stays loopback-only; do not publish 18083 as 0.0.0.0:18083.
      EMQX_DASHBOARD__LISTENERS__HTTP__BIND: "0.0.0.0:18083"
      EMQX_LISTENERS__TCP__DEFAULT__BIND: "0.0.0.0:1883"
      EMQX_AUTHENTICATION__1__MECHANISM: password_based
      EMQX_AUTHENTICATION__1__BACKEND: built_in_database
      EMQX_AUTHENTICATION__1__ENABLE: "true"
      EMQX_NODE__COOKIE: ${EMQX_NODE_COOKIE:?set EMQX_NODE_COOKIE in .env.production}
    volumes:
      - ./emqx/base.hocon:/opt/emqx/etc/base.hocon:ro
      - ./emqx/default_api_key.conf:/opt/emqx/etc/default_api_key.conf:ro
      - ./emqx/certs:/opt/emqx/etc/certs:ro
      - emqx_data:/opt/emqx/data
    networks:
      internal:
        aliases:
          - emqx
      edge: {}
    ports:
      - "8883:8883"
      - "127.0.0.1:18083:18083"
    # Prefer `ping` (lightweight); fall back to `status` for older CLI layouts.
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "/opt/emqx/bin/emqx_ctl ping >/dev/null 2>&1 || /opt/emqx/bin/emqx_ctl status >/dev/null 2>&1",
        ]
      interval: 15s
      timeout: 10s
      retries: 30
      start_period: 90s
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 768M
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  api:
    image: ${IMAGE_REGISTRY:?set IMAGE_REGISTRY in .env.production}/${APP_IMAGE_REPOSITORY:?set APP_IMAGE_REPOSITORY in .env.production}:${APP_IMAGE_TAG:?set APP_IMAGE_TAG in .env.production}
    container_name: avf-prod-api
    restart: unless-stopped
    command: ["/app/api"]
    # Shared prod env: NATS_URL, APP_ENV, TELEMETRY_*, METRICS_*, etc.
    env_file:
      - ${PROD_ENV_FILE:-.env.production}
    environment:
      MQTT_CLIENT_ID: ${MQTT_CLIENT_ID_API:?set MQTT_CLIENT_ID_API in .env.production}
    networks:
      - internal
      - edge
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
      emqx:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "curl -fsS http://127.0.0.1:8080/health/live | grep -qx ok && curl -fsS http://127.0.0.1:8080/health/ready | grep -qx ok",
        ]
      interval: 20s
      timeout: 5s
      retries: 5
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: "0.60"
          memory: 512M
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  worker:
    image: ${IMAGE_REGISTRY:?set IMAGE_REGISTRY in .env.production}/${APP_IMAGE_REPOSITORY:?set APP_IMAGE_REPOSITORY in .env.production}:${APP_IMAGE_TAG:?set APP_IMAGE_TAG in .env.production}
    container_name: avf-prod-worker
    restart: unless-stopped
    command: ["/app/worker"]
    # JetStream projection + metrics/health: NATS_URL, TELEMETRY_*, METRICS_ENABLED, WORKER_METRICS_LISTEN.
    env_file:
      - ${PROD_ENV_FILE:-.env.production}
    networks:
      - internal
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", 'read -r cmd </proc/1/comm && [ "$$cmd" = "worker" ]']
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    deploy:
      resources:
        limits:
          cpus: "0.35"
          memory: 384M
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  mqtt-ingest:
    image: ${IMAGE_REGISTRY:?set IMAGE_REGISTRY in .env.production}/${APP_IMAGE_REPOSITORY:?set APP_IMAGE_REPOSITORY in .env.production}:${APP_IMAGE_TAG:?set APP_IMAGE_TAG in .env.production}
    container_name: avf-prod-mqtt-ingest
    restart: unless-stopped
    command: ["/app/mqtt-ingest"]
    # Bounded MQTT → NATS: NATS_URL, MQTT_*, TELEMETRY_* (ingress + stream limits), METRICS_*.
    env_file:
      - ${PROD_ENV_FILE:-.env.production}
    environment:
      MQTT_CLIENT_ID: ${MQTT_CLIENT_ID_INGEST:-avf-prod-mqtt-ingest}
    networks:
      - internal
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
      emqx:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'read -r cmd </proc/1/comm && [ "$$cmd" = "mqtt-ingest" ]',
        ]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 60s
    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 256M
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  reconciler:
    image: ${IMAGE_REGISTRY:?set IMAGE_REGISTRY in .env.production}/${APP_IMAGE_REPOSITORY:?set APP_IMAGE_REPOSITORY in .env.production}:${APP_IMAGE_TAG:?set APP_IMAGE_TAG in .env.production}
    container_name: avf-prod-reconciler
    restart: unless-stopped
    command: ["/app/reconciler"]
    # Same env_file as other app services for consistent validation; reconciler does not host telemetry projection.
    env_file:
      - ${PROD_ENV_FILE:-.env.production}
    networks:
      - internal
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
    healthcheck:
      test:
        ["CMD-SHELL", 'read -r cmd </proc/1/comm && [ "$$cmd" = "reconciler" ]']
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 256M
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  migrate:
    image: ${IMAGE_REGISTRY:?set IMAGE_REGISTRY in .env.production}/${GOOSE_IMAGE_REPOSITORY:?set GOOSE_IMAGE_REPOSITORY in .env.production}:${GOOSE_IMAGE_TAG:?set GOOSE_IMAGE_TAG in .env.production}
    container_name: avf-prod-migrate
    restart: "no"
    env_file:
      - ${PROD_ENV_FILE:-.env.production}
    networks:
      - internal
    volumes:
      - ../../migrations:/migrations:ro
    depends_on:
      postgres:
        condition: service_healthy
    entrypoint:
      [
        "/bin/sh",
        "-c",
        'exec /usr/local/bin/goose -dir /migrations postgres "$$DATABASE_URL" up',
      ]

  caddy:
    image: caddy:2.8-alpine
    container_name: avf-prod-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      API_DOMAIN: ${API_DOMAIN:-api.ldtv.dev}
      CADDY_ACME_EMAIL: ${CADDY_ACME_EMAIL:?set CADDY_ACME_EMAIL in .env.production for ACME}
      UPSTREAM_API: ${UPSTREAM_API:-api:8080}
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - edge
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "curl -fsS http://api:8080/health/live | grep -qx ok",
        ]
      interval: 30s
      timeout: 5s
      retries: 8
      start_period: 30s
    deploy:
      resources:
        limits:
          cpus: "0.15"
          memory: 128M
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "2"
