# Postman run reports (committed only)

Local Newman CLI output belongs in `.e2e-runs/` (gitignored) unless intentionally archived here for audit evidence.

Do not commit raw tokens or environment secrets in report JSON.
