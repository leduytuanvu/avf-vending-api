$kind: http-request
name: "[PRODUCTION-WRITE] POST /v1/device/machines/{machineId}/commands/poll — Poll pending remote commands over HTTP (MQTT fallback)"
description: |-
  ## Purpose
  Poll pending remote commands over HTTP (MQTT fallback)

  ## Auth mode
  `machine` — required: machineToken

  ## Required variables
  machineToken

  ## Headers
  - Accept: application/json
  - X-Request-ID: {{requestId}}
  - X-Correlation-ID: {{correlationId}}
  - Authorization: Bearer {{machineToken}}
  - X-Request-ID: (from OpenAPI)
  - X-Correlation-ID: (from OpenAPI)

  ## Path parameters
  - `machineId` (string)

  ## Query parameters
  _None_

  ## Required body fields
  _None_

  ## Optional body fields
  _None_

  ## Enum / options
  _None documented in schema_

  ## Success response
  HTTP **200**

  ## Common failure statuses
  `400`, `401`, `403`, `429`, `500`

  ## Safety note
  Mutating — requires allowGatedWrites=true and confirmProductionWrites=I_UNDERSTAND_THIS_WRITES_TO_PRODUCTION. **HIGH RISK** — item disabled by default.

  ## Metadata
  - openapiOperationId: `DocOpV1DeviceMachineCommandsPoll`
  - openapiKey: `POST /v1/device/machines/{machineId}/commands/poll`
  - production_write: yes
  - idempotency: no
url: "{{baseUrl}}/v1/device/machines/{{machineId}}/commands/poll"
method: POST
headers:
  Accept: application/json
  X-Request-ID: "{{requestId}}"
  X-Correlation-ID: "{{correlationId}}"
  Authorization: Bearer {{machineToken}}
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "limit": 10
    }
auth:
  type: bearer
  credentials:
    token: "{{machineToken}}"
scripts:
  - type: beforeRequest
    code: |-
      pm.environment.set('auth_type', 'machine');
      pm.collectionVariables.set('requires_idempotency', 'false');
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test("expected HTTP success unless negative test", function () {
        if (pm.collectionVariables.get('negative_test') === 'true') { return; }
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
examples: ./.resources/[PRODUCTION-WRITE] POST -v1-device-machines-{machineId}-commands.resources/examples
order: 8000
