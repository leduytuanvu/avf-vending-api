$kind: http-request
name: MQTT PUB {{mqttTopicPrefix}}/machines/{{machineId}}/commands — Backend command dispatch
description: |
  **MQTT manual test** (Postman Desktop → New → MQTT)

  - **Broker:** `{{mqttBrokerUrl}}`
  - **Client ID:** `{{mqttClientId}}`
  - **Topic:** `{{mqttTopicPrefix}}/machines/{{machineId}}/commands`
  - **Direction:** backend_publishes
  - **QoS:** {{mqttQos}} | **Retain:** {{mqttRetain}}

  **Payload (accepted):**
  ```json
  {
    "command_id": "{{commandId}}",
    "machine_id": "{{machineId}}",
    "sequence": 1,
    "command_type": "noop",
    "payload": {},
    "idempotency_key": "{{idempotencyKey}}",
    "correlation_id": "{{correlationId}}"
  }
  ```

  **Payload (rejected example):** invalid schema_version or wrong machine_id
url: "{{mqttBrokerUrl}}"
method: MQTT
scripts:
  - type: afterResponse
    code: |-
      pm.test("expected HTTP success unless negative test", function () {
        if (pm.collectionVariables.get('negative_test') === 'true') { return; }
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
order: 1000
