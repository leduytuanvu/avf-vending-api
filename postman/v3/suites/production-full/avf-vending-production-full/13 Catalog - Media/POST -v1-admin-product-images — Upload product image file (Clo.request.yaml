$kind: http-request
name: POST /v1/admin/product-images — Upload product image file (Cloudinary)
description: |-
  ## Purpose
  Upload product image file (Cloudinary)

  ## Auth mode
  `admin` — required: accessToken (set via 02 Auth login)

  ## Required variables
  accessToken (set via 02 Auth login)

  ## Headers
  - Accept: application/json
  - X-Request-ID: {{requestId}}
  - X-Correlation-ID: {{correlationId}}
  - Idempotency-Key: {{idempotencyKey}}
  - Authorization: Bearer {{accessToken}}
  - Idempotency-Key: (from OpenAPI)
  - X-Request-ID: (from OpenAPI)
  - X-Correlation-ID: (from OpenAPI)

  ## Path parameters
  _None_

  ## Query parameters
  _None_

  ## Required body fields
  _None_

  ## Optional body fields
  _None_

  ## Enum / options
  _None documented in schema_

  ## Success response
  HTTP **201**

  ## Common failure statuses
  `400`, `401`, `403`, `413`, `503`

  ## Safety note
  Mutating — requires allowGatedWrites=true and confirmProductionWrites=I_UNDERSTAND_THIS_WRITES_TO_PRODUCTION.

  ## Metadata
  - openapiOperationId: `DocOpV1AdminProductImageUpload`
  - openapiKey: `POST /v1/admin/product-images`
  - production_write: yes
  - idempotency: yes
url: "{{baseUrl}}/v1/admin/product-images"
method: POST
headers:
  Accept: application/json
  X-Request-ID: "{{requestId}}"
  X-Correlation-ID: "{{correlationId}}"
  Idempotency-Key: "{{idempotencyKey}}"
  Authorization: Bearer {{accessToken}}
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
scripts:
  - type: beforeRequest
    code: |-
      pm.environment.set('auth_type', 'admin');
      pm.collectionVariables.set('requires_idempotency', 'true');
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test("expected HTTP success unless negative test", function () {
        if (pm.collectionVariables.get('negative_test') === 'true') { return; }
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
examples: ./.resources/POST -v1-admin-product-images — Upload product image file (Clo.resources/examples
order: 14000
