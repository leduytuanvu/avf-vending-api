$kind: http-request
name: GET /metrics — Prometheus metrics scrape (public listener; optional)
description: |-
  ## Purpose
  Prometheus metrics scrape (public listener; optional)

  ## Auth mode
  `public` — required: none

  ## Required variables
  none

  ## Headers
  - Accept: application/json
  - X-Request-ID: {{requestId}}
  - X-Correlation-ID: {{correlationId}}

  ## Path parameters
  _None_

  ## Query parameters
  _None_

  ## Required body fields
  _None_

  ## Optional body fields
  _None_

  ## Enum / options
  _None documented in schema_

  ## Success response
  HTTP **200**

  ## Common failure statuses
  `401`, `404`

  ## Safety note
  Read-only.

  ## Metadata
  - openapiOperationId: `DocOpMetrics`
  - openapiKey: `GET /metrics`
  - production_write: no
  - idempotency: no
url: "{{baseUrl}}/metrics"
method: GET
headers:
  Accept: application/json
  X-Request-ID: "{{requestId}}"
  X-Correlation-ID: "{{correlationId}}"
auth:
  type: noauth
scripts:
  - type: beforeRequest
    code: |-
      pm.environment.set('auth_type', 'public');
      pm.collectionVariables.set('requires_idempotency', 'false');
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test("expected HTTP success unless negative test", function () {
        if (pm.collectionVariables.get('negative_test') === 'true') { return; }
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
examples: ./.resources/GET -metrics — Prometheus metrics scrape (public listener; opt.resources/examples
order: 3000
