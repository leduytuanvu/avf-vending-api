$kind: http-request
name: gRPC InternalMachineQueryService/GetMachineCabinetSlotSummary — Get Machine Cabinet Slot Summary
description: |
  **gRPC manual test** (Postman Desktop → New → gRPC Request)

  - **fullMethod:** `/avf.v1.InternalMachineQueryService/GetMachineCabinetSlotSummary`
  - **Proto:** `proto/avf/v1/internal_queries.proto`
  - **Target:** `{{grpcTarget}}` (TLS)
  - **Registered:** not-registered

  **Metadata:** authorization: Bearer {{accessToken}}; x-request-id: {{requestId}}; x-correlation-id: {{correlationId}}

  **Request JSON:**
  ```json
  {
    "machine_id": "{{machineId}}"
  }
  ```

  **Response shape:**
  ```json
  {
    "machine": {
      "machine_id": "{{machineId}}",
      "site_id": "{{siteId}}",
      "hardware_profile_id": {},
      "serial_number": "string",
      "name": "string",
      "status": "string",
      "command_sequence": 1,
      "created_at": {},
      "updated_at": {}
    },
    "cabinets": {
      "id": "{{id}}",
      "machine_id": "{{machineId}}",
      "code": "string",
      "title": "string",
      "sort_order": 1,
      "metadata": {},
      "created_at": {},
      "updated_at": {}
    },
    "configured_slots": {
      "config_id": "{{configId}}",
      "cabinet_code": "string",
      "slot_code": "string",
      "slot_index": {},
      "product_id": {},
      "product_sku": "string",
      "product_name": "string",
      "max_quantity": 1,
      "price_minor": 1,
      "effective_from": {},
      "is_current": false,
      "machine_slot_layout_id": "{{machineSlotLayoutId}}"
    },
    "legacy_slots": {
      "planogram_id": "{{planogramId}}",
      "planogram_name": "string",
      "slot_index": 1,
      "current_quantity": 1,
      "max_quantity": 1,
      "price_minor": 1,
      "product_id": {},
      "product_sku": "string",
      "product_name": "string",
      "planogram_revision": 1
    },
    "assortment_products": {
      "product_id": "{{productId}}",
      "sku": "string",
      "name": "string",
      "sort_order": 1,
      "assortment_id": "{{assortmentId}}",
      "assortment_name": "string"
    }
  }
  ```

  **gRPC error statuses:** UNAUTHENTICATED, PERMISSION_DENIED, INVALID_ARGUMENT, NOT_FOUND, FAILED_PRECONDITION, INTERNAL
url: grpcs://{{grpcTarget}}
method: GRPC
scripts:
  - type: afterResponse
    code: |-
      pm.test("expected HTTP success unless negative test", function () {
        if (pm.collectionVariables.get('negative_test') === 'true') { return; }
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
order: 6000
