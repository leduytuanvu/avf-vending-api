$kind: http-request
name: gRPC MachineCommerceService/CreateOrderFromQuote — Create Order From Quote
description: |
  **gRPC manual test** (Postman Desktop → New → gRPC Request)

  - **fullMethod:** `/avf.machine.v1.MachineCommerceService/CreateOrderFromQuote`
  - **Proto:** `proto/avf/machine/v1/commerce.proto`
  - **Target:** `{{grpcTarget}}` (TLS)
  - **Registered:** machine

  **Metadata:** authorization: Bearer {{machineToken}}; x-request-id: {{requestId}}; x-correlation-id: {{correlationId}}

  **Request JSON:**
  ```json
  {
    "context": {
      "idempotency_key": "{{idempotencyKey}}",
      "client_event_id": "{{clientEventId}}",
      "client_created_at": {},
      "operator_session_id": "{{operatorSessionId}}"
    },
    "machine_id": "{{machineId}}",
    "quote_id": "{{quoteId}}",
    "payment_method": "string",
    "meta": {
      "machine_id": "{{machineId}}",
      "request_id": "{{requestId}}",
      "idempotency_key": "{{idempotencyKey}}",
      "occurred_at": {},
      "client_sequence": 1,
      "offline_sequence": 1,
      "app_version": "string",
      "config_version": 1,
      "catalog_version": "string",
      "client_event_id": "{{clientEventId}}"
    },
    "simulation": {
      "simulated": false,
      "simulation_run_id": "{{simulationRunId}}",
      "simulation_scenario": "string",
      "fake_bill": false,
      "fake_board": false
    }
  }
  ```

  **Response shape:**
  ```json
  {
    "replay": false,
    "order_id": "{{orderId}}",
    "order_status": "string",
    "currency": "string",
    "subtotal_minor": 1,
    "tax_minor": 1,
    "total_minor": 1,
    "lines": {
      "vend_session_id": "{{vendSessionId}}",
      "line_sequence": 1,
      "slot_index": 1,
      "product_id": "{{productId}}",
      "cabinet_code": "string",
      "slot_code": "string",
      "vend_state": "string"
    },
    "meta": {
      "server_time": {},
      "trace_id": "{{traceId}}",
      "request_id": "{{requestId}}",
      "status": {},
      "retryable": false,
      "error_code": "string"
    }
  }
  ```

  **gRPC error statuses:** UNAUTHENTICATED, PERMISSION_DENIED, INVALID_ARGUMENT, NOT_FOUND, FAILED_PRECONDITION, INTERNAL
url: grpcs://{{grpcTarget}}
method: GRPC
scripts:
  - type: afterResponse
    code: |-
      pm.test("expected HTTP success unless negative test", function () {
        if (pm.collectionVariables.get('negative_test') === 'true') { return; }
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
order: 29000
