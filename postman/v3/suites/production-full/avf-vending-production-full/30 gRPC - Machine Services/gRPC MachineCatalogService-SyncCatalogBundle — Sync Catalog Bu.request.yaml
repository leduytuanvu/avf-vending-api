$kind: http-request
name: gRPC MachineCatalogService/SyncCatalogBundle — Sync Catalog Bundle
description: |
  **gRPC manual test** (Postman Desktop → New → gRPC Request)

  - **fullMethod:** `/avf.machine.v1.MachineCatalogService/SyncCatalogBundle`
  - **Proto:** `proto/avf/machine/v1/catalog.proto`
  - **Target:** `{{grpcTarget}}` (TLS)
  - **Registered:** machine

  **Metadata:** authorization: Bearer {{machineToken}}; x-request-id: {{requestId}}; x-correlation-id: {{correlationId}}

  **Request JSON:**
  ```json
  {
    "meta": {
      "machine_id": "{{machineId}}",
      "request_id": "{{requestId}}",
      "idempotency_key": "{{idempotencyKey}}",
      "occurred_at": {},
      "client_sequence": 1,
      "offline_sequence": 1,
      "app_version": "string",
      "config_version": 1,
      "catalog_version": "string",
      "client_event_id": "{{clientEventId}}"
    },
    "machine_id": "{{machineId}}",
    "include_unavailable": false,
    "include_images": false,
    "current_catalog_version": "string",
    "current_media_manifest_version": "string",
    "basis_product_ids": "{{basisProductIds}}"
  }
  ```

  **Response shape:**
  ```json
  {
    "meta": {
      "server_time": {},
      "trace_id": "{{traceId}}",
      "request_id": "{{requestId}}",
      "status": {},
      "retryable": false,
      "error_code": "string"
    },
    "catalog_version": "string",
    "media_manifest_version": "string",
    "generated_at": {},
    "machine_id": "{{machineId}}",
    "changed_products": {
      "slot_index": 1,
      "slot_code": "string",
      "cabinet_code": "string",
      "product_id": "{{productId}}",
      "sku": "string",
      "name": "string",
      "short_name": "string",
      "price_minor": 1,
      "available_quantity": 1,
      "max_quantity": 1,
      "is_available": false,
      "unavailable_reason": "string",
      "sort_order": 1,
      "primary_media": {
        "thumb_url": "string",
        "display_url": "string",
        "checksum_sha256": "string",
        "etag": "string",
        "updated_at": {},
        "media_id": "{{mediaId}}",
        "size_bytes": 1,
        "object_version": 1,
        "media_version": 1,
        "width": 1,
        "height": 1,
        "content_type": "string",
        "deleted": false,
        "expires_at": {},
        "media_variants": {
          "kind": {},
          "media_asset_id": "{{mediaAssetId}}",
          "url": "string",
          "content_type": "string",
          "checksum_sha256": "string",
          "etag": "string",
          "size_bytes": 1,
          "width": 1,
          "height": 1,
          "expires_at": {},
          "media_version": 1,
          "updated_at": {}
        },
        "source_type": "string",
        "cache_key": "string",
        "offline_required": false,
        "download_strategy": "string"
      }
    },
    "removed_product_ids": "{{removedProductIds}}",
    "changed_media_assets": {
      "id": "{{id}}",
      "product_id": "{{productId}}",
      "role": "string",
      "variant": "string",
      "mime_type": "string",
      "width": 1,
      "height": 1,
      "sha256": "string",
      "size_bytes": 1,
      "version": 1,
      "download_url": "string"
    },
    "removed_media_asset_ids": "{{removedMediaAssetIds}}"
  }
  ```

  **gRPC error statuses:** UNAUTHENTICATED, PERMISSION_DENIED, INVALID_ARGUMENT, NOT_FOUND, FAILED_PRECONDITION, INTERNAL
url: grpcs://{{grpcTarget}}
method: GRPC
scripts:
  - type: afterResponse
    code: |-
      pm.test("expected HTTP success unless negative test", function () {
        if (pm.collectionVariables.get('negative_test') === 'true') { return; }
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
order: 15000
