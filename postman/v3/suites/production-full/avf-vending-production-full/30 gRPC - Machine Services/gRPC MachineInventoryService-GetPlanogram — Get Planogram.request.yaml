$kind: http-request
name: gRPC MachineInventoryService/GetPlanogram — Get Planogram
description: |
  **gRPC manual test** (Postman Desktop → New → gRPC Request)

  - **fullMethod:** `/avf.machine.v1.MachineInventoryService/GetPlanogram`
  - **Proto:** `proto/avf/machine/v1/inventory.proto`
  - **Target:** `{{grpcTarget}}` (TLS)
  - **Registered:** machine

  **Metadata:** authorization: Bearer {{machineToken}}; x-request-id: {{requestId}}; x-correlation-id: {{correlationId}}

  **Request JSON:**
  ```json
  {
    "machine_id": "{{machineId}}"
  }
  ```

  **Response shape:**
  ```json
  {
    "machine_id": "{{machineId}}",
    "server_time": {},
    "slots": {
      "planogram_id": "{{planogramId}}",
      "planogram_name": "string",
      "slot_index": 1,
      "current_quantity": 1,
      "max_quantity": 1,
      "price_minor": 1,
      "planogram_revision_applied": 1,
      "cabinet_code": "string",
      "cabinet_index": 1,
      "product_id": "{{productId}}",
      "product_sku": "string",
      "product_name": "string",
      "is_empty": false
    }
  }
  ```

  **gRPC error statuses:** UNAUTHENTICATED, PERMISSION_DENIED, INVALID_ARGUMENT, NOT_FOUND, FAILED_PRECONDITION, INTERNAL
url: grpcs://{{grpcTarget}}
method: GRPC
scripts:
  - type: afterResponse
    code: |-
      pm.test("expected HTTP success unless negative test", function () {
        if (pm.collectionVariables.get('negative_test') === 'true') { return; }
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
order: 39000
