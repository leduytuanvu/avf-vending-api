$kind: http-request
name: GET /v1/machines/{machineId}/commands/receipts — List recent command receipts
description: |-
  ## Purpose
  List recent command receipts

  ## Auth mode
  `machine` — required: machineToken

  ## Required variables
  machineToken

  ## Headers
  - Accept: application/json
  - X-Request-ID: {{requestId}}
  - X-Correlation-ID: {{correlationId}}
  - Authorization: Bearer {{machineToken}}
  - X-Request-ID: (from OpenAPI)
  - X-Correlation-ID: (from OpenAPI)

  ## Path parameters
  - `machineId` (string)

  ## Query parameters
  - `limit` (optional, integer)

  ## Required body fields
  _None_

  ## Optional body fields
  _None_

  ## Enum / options
  _None documented in schema_

  ## Success response
  HTTP **200**

  ## Common failure statuses
  `400`, `401`, `403`, `500`

  ## Safety note
  Read-only.

  ## Metadata
  - openapiOperationId: `DocOpV1MachineCommandReceipts`
  - openapiKey: `GET /v1/machines/{machineId}/commands/receipts`
  - production_write: no
  - idempotency: no
url: "{{baseUrl}}/v1/machines/{{machineId}}/commands/receipts"
method: GET
headers:
  Accept: application/json
  X-Request-ID: "{{requestId}}"
  X-Correlation-ID: "{{correlationId}}"
  Authorization: Bearer {{machineToken}}
queryParams:
  - key: limit
    value: "1"
    disabled: true
    description: Default 50, max 500
auth:
  type: bearer
  credentials:
    token: "{{machineToken}}"
scripts:
  - type: beforeRequest
    code: |-
      pm.environment.set('auth_type', 'machine');
      pm.collectionVariables.set('requires_idempotency', 'false');
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test("expected HTTP success unless negative test", function () {
        if (pm.collectionVariables.get('negative_test') === 'true') { return; }
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
examples: ./.resources/GET -v1-machines-{machineId}-commands-receipts — List recent c.resources/examples
order: 4000
