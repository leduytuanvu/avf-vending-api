$kind: http-request
name: GET /v1/admin/payments/reconciliation — Payment reconciliation drift report
description: Gate 0 coverage for OpenAPI `GET /v1/admin/payments/reconciliation` — Payment reconciliation drift report
url: "{{baseUrl}}/v1/admin/payments/reconciliation"
method: GET
headers:
  Accept: application/json
  X-Request-ID: "{{requestId}}"
  X-Correlation-ID: "{{correlationId}}"
  Authorization: Bearer {{accessToken}}
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 11000
