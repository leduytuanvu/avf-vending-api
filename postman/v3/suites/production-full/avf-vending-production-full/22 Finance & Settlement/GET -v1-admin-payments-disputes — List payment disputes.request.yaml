$kind: http-request
name: GET /v1/admin/payments/disputes — List payment disputes
description: Gate 0 coverage for OpenAPI `GET /v1/admin/payments/disputes` — List payment disputes
url: "{{baseUrl}}/v1/admin/payments/disputes"
method: GET
headers:
  Accept: application/json
  X-Request-ID: "{{requestId}}"
  X-Correlation-ID: "{{correlationId}}"
  Authorization: Bearer {{accessToken}}
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 9000
