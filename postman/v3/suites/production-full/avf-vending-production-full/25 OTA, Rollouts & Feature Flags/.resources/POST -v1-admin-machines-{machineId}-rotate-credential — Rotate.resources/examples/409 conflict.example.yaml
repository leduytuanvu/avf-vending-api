$kind: http-example
request:
  url: "{{baseUrl}}/"
  method: GET
response:
  statusCode: 409
  statusText: Error
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "error": {
          "code": "conflict",
          "message": "resource conflict",
          "requestId": "{{requestId}}"
        }
      }
order: 7000
