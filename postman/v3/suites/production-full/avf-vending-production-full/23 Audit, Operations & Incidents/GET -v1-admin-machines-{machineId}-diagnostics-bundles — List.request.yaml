$kind: http-request
name: GET /v1/admin/machines/{machineId}/diagnostics/bundles — List machine diagnostic bundles
description: |-
  ## Purpose
  List machine diagnostic bundles

  ## Auth mode
  `admin` — required: accessToken (set via 02 Auth login)

  ## Required variables
  accessToken (set via 02 Auth login)

  ## Headers
  - Accept: application/json
  - X-Request-ID: {{requestId}}
  - X-Correlation-ID: {{correlationId}}
  - Authorization: Bearer {{accessToken}}
  - X-Request-ID: (from OpenAPI)
  - X-Correlation-ID: (from OpenAPI)

  ## Path parameters
  - `machineId` (string)

  ## Query parameters
  - `limit` (optional, integer)
  - `offset` (optional, integer)

  ## Required body fields
  _None_

  ## Optional body fields
  _None_

  ## Enum / options
  _None documented in schema_

  ## Success response
  HTTP **200**

  ## Common failure statuses
  `400`, `401`, `403`, `500`

  ## Safety note
  Read-only.

  ## Metadata
  - openapiOperationId: `DocOpV1AdminMachineDiagnosticBundlesList`
  - openapiKey: `GET /v1/admin/machines/{machineId}/diagnostics/bundles`
  - production_write: no
  - idempotency: no
url: "{{baseUrl}}/v1/admin/machines/{{machineId}}/diagnostics/bundles"
method: GET
headers:
  Accept: application/json
  X-Request-ID: "{{requestId}}"
  X-Correlation-ID: "{{correlationId}}"
  Authorization: Bearer {{accessToken}}
queryParams:
  - key: limit
    value: "1"
    disabled: true
    description: Default 50, max 500
  - key: offset
    value: "1"
    disabled: true
    description: Default 0
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
scripts:
  - type: beforeRequest
    code: |-
      pm.environment.set('auth_type', 'admin');
      pm.collectionVariables.set('requires_idempotency', 'false');
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test("expected HTTP success unless negative test", function () {
        if (pm.collectionVariables.get('negative_test') === 'true') { return; }
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
examples: ./.resources/GET -v1-admin-machines-{machineId}-diagnostics-bundles — List.resources/examples
order: 7000
