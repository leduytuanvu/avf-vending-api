$kind: http-request
name: GET /v1/admin/machines/{machineId}/runtime-sessions/history — Machine runtime session history (admin)
description: Gate 0 coverage for OpenAPI `GET /v1/admin/machines/{machineId}/runtime-sessions/history` — Machine runtime session history (admin)
url: "{{baseUrl}}/v1/admin/machines/{{machineId}}/runtime-sessions/history"
method: GET
headers:
  Accept: application/json
  X-Request-ID: "{{requestId}}"
  X-Correlation-ID: "{{correlationId}}"
  Authorization: Bearer {{accessToken}}
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 37000
