$kind: http-request
name: GET /v1/admin/machines/{machineId}/runtime-sessions/current — Current machine runtime session (admin)
description: Gate 0 coverage for OpenAPI `GET /v1/admin/machines/{machineId}/runtime-sessions/current` — Current machine runtime session (admin)
url: "{{baseUrl}}/v1/admin/machines/{{machineId}}/runtime-sessions/current"
method: GET
headers:
  Accept: application/json
  X-Request-ID: "{{requestId}}"
  X-Correlation-ID: "{{correlationId}}"
  Authorization: Bearer {{accessToken}}
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 36000
