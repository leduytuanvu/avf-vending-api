$kind: http-request
name: GET /v1/admin/machines/{machineId}/ops-overview — Machine operational overview (admin)
description: Gate 0 coverage for OpenAPI `GET /v1/admin/machines/{machineId}/ops-overview` — Machine operational overview (admin)
url: "{{baseUrl}}/v1/admin/machines/{{machineId}}/ops-overview"
method: GET
headers:
  Accept: application/json
  X-Request-ID: "{{requestId}}"
  X-Correlation-ID: "{{correlationId}}"
  Authorization: Bearer {{accessToken}}
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 35000
