$kind: http-request
name: GET /v1/machines/{machineId}/sale-catalog — Runtime sale catalog (planogram, price, stock, images)
description: |-
  ## Purpose
  Runtime sale catalog (planogram, price, stock, images)

  ## Auth mode
  `machine` — required: machineToken

  ## Required variables
  machineToken

  ## Headers
  - Accept: application/json
  - X-Request-ID: {{requestId}}
  - X-Correlation-ID: {{correlationId}}
  - Authorization: Bearer {{machineToken}}
  - X-Request-ID: (from OpenAPI)
  - X-Correlation-ID: (from OpenAPI)

  ## Path parameters
  - `machineId` (string)

  ## Query parameters
  - `if_none_match_config_version` (optional, integer)
  - `include_unavailable` (optional, string)
  - `include_images` (optional, string)

  ## Required body fields
  _None_

  ## Optional body fields
  _None_

  ## Enum / options
  _None documented in schema_

  ## Success response
  HTTP **200**

  ## Common failure statuses
  `400`, `401`, `403`, `404`, `500`

  ## Safety note
  Read-only.

  ## Metadata
  - openapiOperationId: `DocOpV1MachineSaleCatalogGet`
  - openapiKey: `GET /v1/machines/{machineId}/sale-catalog`
  - production_write: no
  - idempotency: no
url: "{{baseUrl}}/v1/machines/{{machineId}}/sale-catalog"
method: GET
headers:
  Accept: application/json
  X-Request-ID: "{{requestId}}"
  X-Correlation-ID: "{{correlationId}}"
  Authorization: Bearer {{machineToken}}
queryParams:
  - key: if_none_match_config_version
    value: "1"
    disabled: true
    description: When equal to current configVersion, respond 304
  - key: include_unavailable
    value: string
    disabled: true
    description: Default false
  - key: include_images
    value: string
    disabled: true
    description: Default true
auth:
  type: bearer
  credentials:
    token: "{{machineToken}}"
scripts:
  - type: beforeRequest
    code: |-
      pm.environment.set('auth_type', 'machine');
      pm.collectionVariables.set('requires_idempotency', 'false');
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test("expected HTTP success unless negative test", function () {
        if (pm.collectionVariables.get('negative_test') === 'true') { return; }
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
examples: ./.resources/GET -v1-machines-{machineId}-sale-catalog — Runtime sale catal.resources/examples
order: 12000
