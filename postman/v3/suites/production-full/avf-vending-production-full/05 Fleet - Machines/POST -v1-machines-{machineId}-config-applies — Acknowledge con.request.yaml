$kind: http-request
name: POST /v1/machines/{machineId}/config-applies — Acknowledge config applied on device
description: |-
  ## Purpose
  Acknowledge config applied on device

  ## Auth mode
  `machine` — required: machineToken

  ## Required variables
  machineToken

  ## Headers
  - Accept: application/json
  - X-Request-ID: {{requestId}}
  - X-Correlation-ID: {{correlationId}}
  - Authorization: Bearer {{machineToken}}
  - X-Request-ID: (from OpenAPI)
  - X-Correlation-ID: (from OpenAPI)

  ## Path parameters
  - `machineId` (string)

  ## Query parameters
  _None_

  ## Required body fields
  _None_

  ## Optional body fields
  _None_

  ## Enum / options
  _None documented in schema_

  ## Success response
  HTTP **201**

  ## Common failure statuses
  `400`, `401`, `403`, `429`, `500`

  ## Safety note
  Mutating — requires allowGatedWrites=true and confirmProductionWrites=I_UNDERSTAND_THIS_WRITES_TO_PRODUCTION.

  ## Metadata
  - openapiOperationId: `DocOpV1MachineConfigApply`
  - openapiKey: `POST /v1/machines/{machineId}/config-applies`
  - production_write: yes
  - idempotency: no
url: "{{baseUrl}}/v1/machines/{{machineId}}/config-applies"
method: POST
headers:
  Accept: application/json
  X-Request-ID: "{{requestId}}"
  X-Correlation-ID: "{{correlationId}}"
  Authorization: Bearer {{machineToken}}
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "android_id": "device-android-1",
      "app_version": "1.0.0",
      "applied_at": "2026-04-19T12:05:00Z",
      "config_payload": {
        "applied_revision": 7
      },
      "config_version": 7
    }
auth:
  type: bearer
  credentials:
    token: "{{machineToken}}"
scripts:
  - type: beforeRequest
    code: |-
      pm.environment.set('auth_type', 'machine');
      pm.collectionVariables.set('requires_idempotency', 'false');
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test("expected HTTP success unless negative test", function () {
        if (pm.collectionVariables.get('negative_test') === 'true') { return; }
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
examples: ./.resources/POST -v1-machines-{machineId}-config-applies — Acknowledge con.resources/examples
order: 25000
