$kind: collection
name: "{{mqttTopicPrefix}}/machines/+/commands/ack"
description: |
  **MQTT manual test item** (Postman Desktop ΓåÆ New ΓåÆ MQTT)

  - **Topic:** `{{mqttTopicPrefix}}/machines/+/commands/ack`
  - **Direction:** machine_publishes_backend_subscribes
  - **Host:** `{{mqttHost}}:{{mqttPort}}`

  **Payload:**
  ```json
  {
    "schema_version": 1,
    "event_id": "{{$guid}}",
    "machine_id": "{{machineId}}",
    "dedupe_key": "avf-postman-{{$guid}}",
    "event_type": "ack",
    "payload": {}
  }
  ```
order: 3000
