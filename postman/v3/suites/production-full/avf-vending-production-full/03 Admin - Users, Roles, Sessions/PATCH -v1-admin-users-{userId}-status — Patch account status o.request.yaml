$kind: http-request
name: PATCH /v1/admin/users/{userId}/status — Patch account status only — alternate path
description: |-
  ## Purpose
  Patch account status only — alternate path

  ## Auth mode
  `admin` — required: accessToken (set via 02 Auth login)

  ## Required variables
  accessToken (set via 02 Auth login)

  ## Headers
  - Accept: application/json
  - X-Request-ID: {{requestId}}
  - X-Correlation-ID: {{correlationId}}
  - Authorization: Bearer {{accessToken}}
  - X-Request-ID: (from OpenAPI)
  - X-Correlation-ID: (from OpenAPI)

  ## Path parameters
  - `userId` (string)

  ## Query parameters
  _None_

  ## Required body fields
  - `status` (string)

  ## Optional body fields
  _None_

  ## Enum / options
  - `status`: `active`, `disabled`, `locked`, `invited`

  ## Success response
  HTTP **200**

  ## Common failure statuses
  `400`, `401`, `403`, `404`, `409`, `500`

  ## Safety note
  Mutating — requires allowGatedWrites=true and confirmProductionWrites=I_UNDERSTAND_THIS_WRITES_TO_PRODUCTION.

  ## Metadata
  - openapiOperationId: `DocOpV1AdminUsersPatchStatus`
  - openapiKey: `PATCH /v1/admin/users/{userId}/status`
  - production_write: yes
  - idempotency: no
url: "{{baseUrl}}/v1/admin/users/{{userId}}/status"
method: PATCH
headers:
  Accept: application/json
  X-Request-ID: "{{requestId}}"
  X-Correlation-ID: "{{correlationId}}"
  Authorization: Bearer {{accessToken}}
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "status": "disabled"
    }
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
scripts:
  - type: beforeRequest
    code: |-
      pm.environment.set('auth_type', 'admin');
      pm.collectionVariables.set('requires_idempotency', 'false');
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test("expected HTTP success unless negative test", function () {
        if (pm.collectionVariables.get('negative_test') === 'true') { return; }
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
examples: ./.resources/PATCH -v1-admin-users-{userId}-status — Patch account status o.resources/examples
order: 13000
