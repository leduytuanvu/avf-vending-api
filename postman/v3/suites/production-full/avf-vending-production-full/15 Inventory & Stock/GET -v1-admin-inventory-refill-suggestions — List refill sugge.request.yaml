$kind: http-request
name: GET /v1/admin/inventory/refill-suggestions — List refill suggestions across machines (all slots)
description: |-
  ## Purpose
  List refill suggestions across machines (all slots)

  ## Auth mode
  `admin` — required: accessToken (set via 02 Auth login)

  ## Required variables
  accessToken (set via 02 Auth login)

  ## Headers
  - Accept: application/json
  - X-Request-ID: {{requestId}}
  - X-Correlation-ID: {{correlationId}}
  - Authorization: Bearer {{accessToken}}
  - X-Request-ID: (from OpenAPI)
  - X-Correlation-ID: (from OpenAPI)

  ## Path parameters
  _None_

  ## Query parameters
  - `site_id` (optional, string)
  - `machine_id` (optional, string)
  - `product_id` (optional, string)
  - `velocity_days` (optional, integer)
  - `urgency` (optional, string)
  - `days_threshold` (optional, string)
  - `limit` (optional, integer)
  - `offset` (optional, integer)

  ## Required body fields
  _None_

  ## Optional body fields
  _None_

  ## Enum / options
  _None documented in schema_

  ## Success response
  HTTP **200**

  ## Common failure statuses
  `400`, `401`, `403`, `500`

  ## Safety note
  Read-only.

  ## Metadata
  - openapiOperationId: `DocOpV1AdminInventoryRefillSuggestions`
  - openapiKey: `GET /v1/admin/inventory/refill-suggestions`
  - production_write: no
  - idempotency: no
url: "{{baseUrl}}/v1/admin/inventory/refill-suggestions"
method: GET
headers:
  Accept: application/json
  X-Request-ID: "{{requestId}}"
  X-Correlation-ID: "{{correlationId}}"
  Authorization: Bearer {{accessToken}}
queryParams:
  - key: site_id
    value: "{{siteId}}"
    disabled: true
    description: Filter by site UUID
  - key: machine_id
    value: "{{machineId}}"
    disabled: true
    description: Filter by machine UUID
  - key: product_id
    value: "{{productId}}"
    disabled: true
    description: Filter by product UUID
  - key: velocity_days
    value: "1"
    disabled: true
    description: Sales lookback window in days (default 14, min 7, max 90)
  - key: urgency
    value: string
    disabled: true
    description: "Filter by urgency: critical, high, medium, low"
  - key: days_threshold
    value: string
    disabled: true
    description: Include rows at or below this estimated days-to-empty
  - key: limit
    value: "1"
    disabled: true
    description: Page size (default 50, max 500)
  - key: offset
    value: "1"
    disabled: true
    description: Offset
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
scripts:
  - type: beforeRequest
    code: |-
      pm.environment.set('auth_type', 'admin');
      pm.collectionVariables.set('requires_idempotency', 'false');
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test("expected HTTP success unless negative test", function () {
        if (pm.collectionVariables.get('negative_test') === 'true') { return; }
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
examples: ./.resources/GET -v1-admin-inventory-refill-suggestions — List refill sugge.resources/examples
order: 3000
