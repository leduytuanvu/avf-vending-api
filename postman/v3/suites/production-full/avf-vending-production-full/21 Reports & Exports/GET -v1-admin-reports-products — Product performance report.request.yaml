$kind: http-request
name: GET /v1/admin/reports/products — Product performance report
description: |-
  ## Purpose
  Product performance report

  ## Auth mode
  `admin` — required: accessToken (set via 02 Auth login)

  ## Required variables
  accessToken (set via 02 Auth login)

  ## Headers
  - Accept: application/json
  - X-Request-ID: {{requestId}}
  - X-Correlation-ID: {{correlationId}}
  - Authorization: Bearer {{accessToken}}
  - X-Request-ID: (from OpenAPI)
  - X-Correlation-ID: (from OpenAPI)

  ## Path parameters
  _None_

  ## Query parameters
  - `from` (required, string)
  - `to` (required, string)
  - `site_id` (optional, string)
  - `machine_id` (optional, string)
  - `product_id` (optional, string)
  - `format` (optional, string)

  ## Required body fields
  _None_

  ## Optional body fields
  _None_

  ## Enum / options
  _None documented in schema_

  ## Success response
  HTTP **200**

  ## Common failure statuses
  `400`, `401`, `403`

  ## Safety note
  Read-only.

  ## Metadata
  - openapiOperationId: `DocOpV1AdminReportsProducts`
  - openapiKey: `GET /v1/admin/reports/products`
  - production_write: no
  - idempotency: no
url: "{{baseUrl}}/v1/admin/reports/products?from=string&to=string"
method: GET
headers:
  Accept: application/json
  X-Request-ID: "{{requestId}}"
  X-Correlation-ID: "{{correlationId}}"
  Authorization: Bearer {{accessToken}}
queryParams:
  - key: from
    value: string
    description: Inclusive window start (RFC3339)
  - key: to
    value: string
    description: Exclusive window end (RFC3339)
  - key: site_id
    value: "{{siteId}}"
    disabled: true
    description: Site UUID filter
  - key: machine_id
    value: "{{machineId}}"
    disabled: true
    description: Machine UUID filter
  - key: product_id
    value: "{{productId}}"
    disabled: true
    description: Product UUID filter
  - key: format
    value: string
    disabled: true
    description: csv
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
scripts:
  - type: beforeRequest
    code: |-
      pm.environment.set('auth_type', 'admin');
      pm.collectionVariables.set('requires_idempotency', 'false');
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test("expected HTTP success unless negative test", function () {
        if (pm.collectionVariables.get('negative_test') === 'true') { return; }
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
examples: ./.resources/GET -v1-admin-reports-products — Product performance report.resources/examples
order: 5000
