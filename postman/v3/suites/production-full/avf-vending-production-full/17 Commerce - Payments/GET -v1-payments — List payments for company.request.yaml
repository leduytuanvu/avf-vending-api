$kind: http-request
name: GET /v1/payments — List payments for company
description: |-
  ## Purpose
  List payments for company

  ## Auth mode
  `machine` — required: machineToken

  ## Required variables
  machineToken

  ## Headers
  - Accept: application/json
  - X-Request-ID: {{requestId}}
  - X-Correlation-ID: {{correlationId}}
  - Authorization: Bearer {{machineToken}}
  - X-Request-ID: (from OpenAPI)
  - X-Correlation-ID: (from OpenAPI)

  ## Path parameters
  _None_

  ## Query parameters
  - `status` (optional, string)
  - `payment_method` (optional, string)
  - `machine_id` (optional, string)
  - `search` (optional, string)
  - `from` (optional, string)
  - `to` (optional, string)
  - `limit` (optional, integer)
  - `offset` (optional, integer)

  ## Required body fields
  _None_

  ## Optional body fields
  _None_

  ## Enum / options
  _None documented in schema_

  ## Success response
  HTTP **200**

  ## Common failure statuses
  `400`, `401`, `403`, `500`

  ## Safety note
  Read-only.

  ## Metadata
  - openapiOperationId: `DocOpV1PaymentsList`
  - openapiKey: `GET /v1/payments`
  - production_write: no
  - idempotency: no
url: "{{baseUrl}}/v1/payments"
method: GET
headers:
  Accept: application/json
  X-Request-ID: "{{requestId}}"
  X-Correlation-ID: "{{correlationId}}"
  Authorization: Bearer {{machineToken}}
queryParams:
  - key: status
    value: string
    disabled: true
    description: Payment state filter (created, authorized, captured, failed, refunded)
  - key: payment_method
    value: string
    disabled: true
    description: Provider filter (e.g. stripe)
  - key: machine_id
    value: "{{machineId}}"
    disabled: true
    description: Filter by order.machine_id
  - key: search
    value: string
    disabled: true
    description: Substring search on ids / idempotency key
  - key: from
    value: string
    disabled: true
    description: created_at lower bound (RFC3339)
  - key: to
    value: string
    disabled: true
    description: created_at upper bound (RFC3339)
  - key: limit
    value: "1"
    disabled: true
    description: Page size (default 50, max 500)
  - key: offset
    value: "1"
    disabled: true
    description: Row offset
auth:
  type: bearer
  credentials:
    token: "{{machineToken}}"
scripts:
  - type: beforeRequest
    code: |-
      pm.environment.set('auth_type', 'machine');
      pm.collectionVariables.set('requires_idempotency', 'false');
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test("expected HTTP success unless negative test", function () {
        if (pm.collectionVariables.get('negative_test') === 'true') { return; }
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
examples: ./.resources/GET -v1-payments — List payments for company.resources/examples
order: 6000
