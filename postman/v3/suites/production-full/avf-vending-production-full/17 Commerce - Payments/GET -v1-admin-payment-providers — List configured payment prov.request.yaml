$kind: http-request
name: GET /v1/admin/payment/providers — List configured payment providers (read-only registry)
description: Gate 0 coverage for OpenAPI `GET /v1/admin/payment/providers` — List configured payment providers (read-only registry)
url: "{{baseUrl}}/v1/admin/payment/providers"
method: GET
headers:
  Accept: application/json
  X-Request-ID: "{{requestId}}"
  X-Correlation-ID: "{{correlationId}}"
  Authorization: Bearer {{accessToken}}
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 8000
