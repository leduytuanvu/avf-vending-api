$kind: http-request
name: 12 POST /v1/admin/machines/{machineId}/activation-codes
description: Mint activation code; captures activationCode.
url: "{{baseUrl}}/v1/admin/machines/{{machineId}}/activation-codes"
method: POST
body:
  type: json
  content: |-
    {
      "expiresInMinutes": 1440,
      "maxUses": 1,
      "notes": "integrated offline-cache suite"
    }
order: 12000
