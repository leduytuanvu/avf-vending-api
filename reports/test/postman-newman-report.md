# Postman / Newman validation (organization removal)

Date: 2026-05-16  
Repo: `avf-vending-api`

## Summary

| Area | Result |
|------|--------|
| Regenerate collection + environments | **PASS** (`tools/build_postman_collection.py`; OpenAPI regenerated via `tools/build_openapi.py` earlier in session) |
| `python -m json.tool` on all `*.postman_collection.json` / `*.postman_environment.json` | **PASS** (7 files) |
| Strict grep (zero hits) in `docs/postman/` and `postman/` | **PASS** |
| `POST /v1/admin/sites` is a real request (not folder-only), no `organization_id` query | **PASS** |
| Environments: no legacy scope variables matching forbidden keys | **PASS** |
| Destructive / production gates still present | **PASS** |
| Local canary gate for `POST /v1/admin/sites` | **PASS** (verified via Newman: blocked without flags, allowed with `allow_destructive=true`) |
| Newman CLI runs | **PASS** (exit 0 for intended folders); **live API not verified** — requests returned **404** against `http://localhost:8080` (listener present, not AVF routes) |
| Newman JSON reporter export | **Blocked / partial** — see below |

**Overall: PASS** for importability, JSON validity, forbidden-token cleanliness, structural checks, and gate behavior. **FAIL / not assessed** for successful HTTP responses against a running AVF API on this host.

## Generated artifact paths

Regenerated by `python tools/build_postman_collection.py` (sources: `tools/postman/collection_prerequest.js`, `collection_test.js`):

| Artifact |
|----------|
| `docs/postman/avf-vending-api.postman_collection.json` |
| `docs/postman/avf-vending-api-function-path.postman_collection.json` (copy of main collection) |
| `docs/postman/avf-local.postman_environment.json` |
| `docs/postman/avf-staging.postman_environment.json` |
| `docs/postman/avf-production.postman_environment.json` |

**Not regenerated:** `postman/full-production-suite/AVF_REST_365_FULL.postman_collection.json` and `postman/full-production-suite/AVF_PRODUCTION.postman_environment.json` (separate generator). They remain grep-clean under the patterns below.

## JSON validation

Command pattern: `python -m json.tool <file>` for every:

- `*.postman_collection.json` (3 files under repo root),
- `*.postman_environment.json` (4 files).

Result: **all parsed successfully** (7 files).

Offline CI-style check: `python tools/check_postman_artifacts.py` → **OK**.

## Strict grep

Patterns (must be zero hits in Postman trees):

`organization`, `organization_id`, `organizationId`, `org_admin`, `tenant`, `canary_organization_id`

Scopes: `docs/postman/`, `postman/`.

Result: **zero hits** (collection descriptions were written to avoid embedding forbidden substrings).

## Structural verification

- **`POST /v1/admin/sites`**: Exactly **one** request item per generated collection; folder trail `Canary admin writes / POST /v1/admin/sites`; `request.method` = `POST`; `request.url` string `{{base_url}}/v1/admin/sites`; **no** `query` array / params (no scope query keys).
- **Environments** (`avf-local`, `avf-staging`, `avf-production`): No variable keys matching the forbidden list above.
- **Destructive gates (existing)**:
  - Production writes still require `allow_mutation`, `allow_production_mutation`, and `confirm_production_run=I_UNDERSTAND_PRODUCTION_MUTATION` (prerequest in generated collections).
  - Staging/production payment + MQTT prefix checks unchanged.
- **Local canary gate (new)**: On `app_env=development` / localhost base URL, `POST` to `/v1/admin/sites` (collection root, not `…/sites/{{id}}`) requires `allow_destructive=true` **or** `canaryMode=true`.
- **Environment variables added** (defaults `false`): `allow_destructive`, `canaryMode` on local, staging, and production exports.

## Newman

- **Installed:** Newman **6.2.2** (`newman --version`).
- **Read-only smoke:**  
  `newman run docs/postman/avf-vending-api-function-path.postman_collection.json -e docs/postman/avf-local.postman_environment.json --folder Public`  
  → exit **0**. Responses were **404** for all four requests (host responds but not with AVF paths).
- **Canary write (gates on):**  
  Same collection/env, `--folder "Canary admin writes"`, `--env-var allow_destructive=true`, `--env-var auth_type=admin`, `--env-var admin_token=newman-canary-placeholder`  
  → exit **0**; prerequest passed; **404** on POST (same infra note).
- **Gate negative test:** Same as above but `--env-var allow_destructive=false` and `--env-var canaryMode=false` → prerequest **Error**, Newman exit **1** (expected).

**Reporter note:** On Windows PowerShell, `--reporters cli,json` was parsed as a single reporter name (`cli json`). Using `--reporters cli json` ran successfully but **`--reporter-json-export` did not produce a file** in this environment (likely missing `newman-reporter-json` package or path handling). CLI transcripts are under `reports/test/postman-newman-evidence/` (`newman-readonly-cli2.log`, `newman-canary-cli2.log`, `newman-canary-gate-blocked.log`).

## Evidence paths

- `reports/test/postman-newman-evidence/newman-readonly-cli2.log` — Public folder run  
- `reports/test/postman-newman-evidence/newman-canary-cli2.log` — Canary folder with `allow_destructive=true`  
- `reports/test/postman-newman-evidence/newman-canary-gate-blocked.log` — Expected prerequest failure without flags  
- `reports/test/postman-newman-evidence/make-*.log` — optional Makefile attempts (no `make` on PATH in this shell)

## Final verdict

- **PASS:** Regenerated docs Postman artifacts, JSON validity, forbidden-token grep, `POST /v1/admin/sites` structure, env cleanliness, production mutation guards, local sites POST gate, Newman CLI execution for scoped folders.  
- **Not PASS (environment):** HTTP-level success against a real AVF API was **not** observed (404 on `:8080`). Re-run Newman against a running API with valid `admin_token` for end-to-end semantics.
