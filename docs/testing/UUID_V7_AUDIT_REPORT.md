# UUID v7 Audit Report

Generated: 2026-05-20 | Branch: `chore/final-full-system-verification-uuidv7-postman-tests`

## Policy

See `docs/architecture/UUID_V7_POLICY.md`. Internal resource IDs generated by application code and database defaults must use **UUID v7** (`internal/platform/id.NewUUIDV7()` / `public.uuid_generate_v7()`).

## Static audit

```bash
bash scripts/audit/verify-uuid-v7.sh
# or: bash scripts/checks/check-uuid-v7.sh
```

**Result: PASS**

```
check-uuid-v7: scanning Go (non-test production paths)
check-uuid-v7: scanning migrations/*.sql goose Up sections
check-uuid-v7: scanning db/schema/*.sql (sqlc mirror)
check-uuid-v7: OK (no forbidden internal UUID v4 generation detected)
verify-uuid-v7: PASS
```

## Go production adopters

| Package | Usage |
|---------|-------|
| `internal/platform/id` | `NewUUIDV7()`, `NewUUIDV7String()` |
| `internal/app/auth` | Refresh token row PK, admin session IDs |
| `internal/app/auth/admin_users.go` | Password reset token PK |
| `internal/app/mediaadmin` | Media asset ID |
| `internal/app/artifacts` | Artifact reservation ID |

## Intentional exceptions (allowlisted)

| Location | Reason |
|----------|--------|
| JWT `jti` (`token_issuer.go`) | Opaque token identifier, not DB PK |
| Request/correlation IDs | Ephemeral tracing, not persisted resource PK |
| Idempotency key strings | Opaque strings, not UUID PKs |
| `migrations/00002_*.sql`, `00004_*.sql` (goose Up) | Historical v4 defaults superseded by `00005` |
| `tools/loadtest/` | Load-test tooling only |
| `*_test.go` | Test fixtures may use `uuid.NewString()` for idempotency keys |

Allowlist file: `scripts/checks/uuid-v7-allowlist.txt`

## Database

| Item | Status |
|------|--------|
| Function `public.uuid_generate_v7()` | Created in `00005_uuid_v7_defaults.sql` |
| Columns converted | **91** `id` columns (catalog-driven ALTER DEFAULT) |
| sqlc mirror `db/schema/01_platform.sql` | Uses `public.uuid_generate_v7()` defaults |
| Fresh insert version nibble | **7** (verified on `avf_vending_test_full_verify`) |

### Migration fix (this pass)

Goose was splitting plpgsql `$$` bodies on internal semicolons. Fixed with `-- +goose StatementBegin/End` wrappers in `00005_uuid_v7_defaults.sql`.

## Integration test

```bash
TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:15432/avf_vending_test_full_verify?sslmode=disable \
  go test -count=1 ./internal/modules/postgres/... -run TestUUIDV7DefaultOnInsert
```

**Result: PASS**

## Postman / OpenAPI examples

- Collection prerequest: `uuid7()` + `{{resource_uuid}}` in `postman/scripts/collection_prerequest.js`
- Generated via `tools/build_postman_collection.py`

## Files changed (UUID v7 scope)

- `internal/platform/id/` (new)
- `migrations/00005_uuid_v7_defaults.sql` (StatementBegin fix)
- `db/schema/01_platform.sql`
- `scripts/checks/check-uuid-v7.sh`, `uuid-v7-allowlist.txt`
- `scripts/audit/verify-uuid-v7.sh` (new)
- ~57 test files: `uuid.New()` → `id.NewUUIDV7()`
- Postman collection prerequest UUID v7 helper

## Verdict

**UUID v7 enforcement: PASS** — static audit clean; DB defaults v7 on fresh migration; integration test confirms version 7 on insert.
