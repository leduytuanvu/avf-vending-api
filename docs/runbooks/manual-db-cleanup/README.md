# Manual database cleanup

One-off SQL helpers that used to live in this folder have been removed.

Provision and upgrade databases using only the goose migrations in `migrations/` at the repository root.
