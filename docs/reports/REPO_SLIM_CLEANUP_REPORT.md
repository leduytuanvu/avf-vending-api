# Repository slim cleanup report

**Branch:** `chore/slim-repo-for-repomix`
**Purpose:** Smaller Repomix exports for ChatGPT review without changing API, migrations, deploy, or production Postman behavior.

## Size

| Metric | Before | After |
|--------|--------|-------|
| Tracked files | 1,679 | 1,676 |
| Tracked bytes (git ls-files) | 33,900,554 | 33,888,829 |
| Untracked local junk removed from disk | ~14.8 MB (118 files) | 0 |

Repomix slim export excludes ~20 MB+ of **tracked** generated Postman/OpenAPI/report content via `.repomixignore` / `repomix.config.slim.json` while keeping sources in git.

## Deleted (tracked)

| Path | Reason |
|------|--------|
| `docs/reports/test/mqtt-full-coverage.json` | Regenerated by `scripts/test/run-mqtt-full-coverage.sh` |
| `docs/reports/verification/EXTERNAL_PRODUCT_IMAGE_URL_AUDIT.md` | Superseded by `EXTERNAL_PRODUCT_IMAGE_URL_FULL_TEST_FINAL.md` |
| `docs/reports/verification/EXTERNAL_PRODUCT_IMAGE_URL_FULL_TEST_AUDIT.md` | Superseded by FINAL |

## Deleted (untracked local only)

- `.tmp-deploy-artifacts/`, `.tmp-postman-backup/`, `.tmp-prod-artifacts/`, `.tmp-uuid/`
- Root scratch: `.tmp-*.js`, `.tmp-*.json`, `.tmp-*.py`, `.tmp-canary.png`, etc.
- Untracked duplicate deploy verification notes under `docs/reports/verification/PRODUCTION_DEPLOY_*_LATEST.md`
- `postman/environments/AVF Production.environment.yaml` (duplicate of production-full-suite env)

## Changed / added

| File | Change |
|------|--------|
| `.gitignore` | Broader ignore for `.tmp-*/`, repomix outputs, coverage, test logs |
| `.repomixignore` | Repomix-only excludes for generated/heavy paths |
| `repomix.config.json` | Default full export config |
| `repomix.config.slim.json` | Slim source-review export config |
| `docs/runbooks/repomix-slim-export.md` | How to run full vs slim Repomix |
| `tmp/repo-cleanup-audit.md` | Pre-cleanup audit (gitignored, local only) |

## Intentionally kept

- `migrations/`, `proto/`, `internal/`, `cmd/`, `deployments/prod/`, `.github/workflows/`
- `postman/production-full-suite/` (canonical production flow)
- `postman/suites/`, `postman/generated/` (large but referenced by generators/CI)
- `docs/swagger/swagger.json` (generated; excluded from slim Repomix only)
- `docs/runbooks/`, `docs/testing/`, `docs/reports/verification/` (historical reports except superseded duplicates above)
- `docs/audits/`

## Repomix slim command

```bash
npx repomix -c repomix.config.slim.json -o repomix-output-slim.xml
```

Full export:

```bash
npx repomix -c repomix.config.json -o repomix-output.xml
```

See `docs/runbooks/repomix-slim-export.md`.

## Test results

| Command | Result |
|---------|--------|
| `go test ./internal/app/mediaadmin/...` | **PASS** |
| `go test ./internal/httpserver/...` | **PASS** |
| `go test ./internal/platform/...` | **PASS** |
| `go test ./cmd/...` | **PASS** |
| `go test ./...` | **FAIL** — `internal/modules/postgres` (environment-blocked: no `TEST_DATABASE_URL` / live Postgres) |
| `go vet ./...` | **PASS** |
| `gofmt -l internal cmd pkg` | **PASS** (no output) |
| `python postman/production-full-suite/validate_product_flow_suite.py` | **PASS** |
| Postman JSON parse | **PASS** |
| `docker compose config` (prod compose) | **BLOCKED** — requires `API_DOMAIN`, `APP_IMAGE_REF` env (no local prod secrets) |

## Manual review (not deleted)

- `postman/suites/full-production-suite/` — exclude from Repomix, keep for full inventory generation
- `postman/generated/` — exclude from Repomix, keep as generator output
- `docs/reports/verification/*` — historical artifacts; trim further only if a newer consolidated index is agreed
- `docs/audits/DEEP_REPO_CLEANUP_*` — prior cleanup history; superseded by this report for slim-export workflow

## Open PR

```bash
gh pr create --base develop --head chore/slim-repo-for-repomix \
  --title "chore: slim repository for repomix review" \
  --body "Adds repomix slim config/ignore, removes local junk and regeneratable report artifacts. No application logic changes."
```
