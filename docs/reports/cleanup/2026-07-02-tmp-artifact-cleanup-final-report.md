# 2026-07-02 tmp / local artifact cleanup final report

**Branch:** `cleanup/local-artifacts-and-tmp-files`  
**Commit SHA (base):** `73a06c3beeeba4cf82209499b9a8315cd6ea75cf`  
**Date:** 2026-07-02

## 1. Summary

Safely removed **~188 MB** of gitignored local temp, test, e2e, and repomix artifacts from the working tree. No tracked source files were deleted. Added explicit `.gitignore` entries for `.tmp/` and global Newman outputs, plus developer cleanup scripts with dry-run default.

## 2. What was inspected

- Git state: branch, SHA, `git ls-files` (1813 tracked files), `git status --short --ignored`
- Ignore rules: [`.gitignore`](../../../.gitignore), [`.dockerignore`](../../../.dockerignore), [`repomix.config.json`](../../../repomix.config.json)
- Artifact directories via depth-limited scan and pattern search
- Reference checks via `rg` for `.tmp`, `.test-runs`, `.e2e-runs`, `migration-evidence`
- Mandatory `git ls-files` check for every deletion candidate
- Pre/post verification: `go test`, `go vet`, CI scripts, Python checkers

## 3. What was deleted (local artifacts only)

| Path | Items (approx) | Size (approx) | Tracked? |
|------|----------------|---------------|----------|
| `.tmp/` | 63 | 0.12 MB | No |
| `.tmp-sec-verdict/` | 1 | 0.01 MB | No |
| `.test-runs/` | 209 | 71.45 MB | No |
| `.e2e-runs/` | 15059 | 18.13 MB | No |
| `tests/e2e/.e2e-runs/` | 7139 | 98.35 MB | No |
| `repomix-output-avf-vending-api.xml` | 1 file | ~15.5 MB | No |
| `migration-evidence/migration-safety-report.json` | 1 | <1 KB | No (regenerated by verify_migrations) |

**Total reclaimed:** ~**188 MB**, ~22,472 files.

## 4. What was intentionally kept

- All tracked source: `cmd/`, `internal/`, `internal/gen/`, `db/`, `migrations/` (15), `proto/`, workflows (20), Postman CI assets, e2e fixtures, `testdata/`, docs, scripts
- `migration-evidence/` directory (empty after JSON delete; CI artifact upload path preserved)
- Gitignored secrets and env files (`.env`, `.env.local`, etc.) — not cleanup targets
- Gitignored `docs/testing/production-e2e/RESULTS_*.md` — regenerable but outside this pass scope

## 5. Tracked candidates not deleted (and why)

None identified. All deleted paths returned empty from `git ls-files`.

## 6. `.gitignore` changes

Added to [`.gitignore`](../../../.gitignore):

- `.tmp/` — explicit directory entry (previously matched only via `*.tmp`)
- `**/newman-report.json` — global Newman JSON output
- `**/newman-junit.xml` — global Newman JUnit output

Existing patterns already covered: `.tmp-*`, `.go-tmp/`, `tmp/`, `temp/`, `.cache/`, `.test-runs/`, `.e2e-runs/`, `tests/e2e/.e2e-runs/`, `ci-reports/`, `security-reports/`, `coverage/`, `bin/`, `dist/`, `repomix-output*.xml`, logs, backups.

## 7. Cleanup script

**Added:**

- [`scripts/local/clean-local-artifacts.sh`](../../../scripts/local/clean-local-artifacts.sh) — dry-run default; `--apply` to delete
- [`scripts/local/clean-local-artifacts.ps1`](../../../scripts/local/clean-local-artifacts.ps1) — dry-run default; `-Apply` to delete

Both refuse tracked paths via `git ls-files`. Documented in [`scripts/README.md`](../../../scripts/README.md).

## 8. Commands run

| Command | Pre-cleanup | Post-cleanup |
|---------|-------------|--------------|
| `go test ./... -short` | PASS | PASS |
| `go vet ./...` | PASS | PASS |
| `bash scripts/ci/verify_migrations.sh` | PASS | PASS |
| `bash scripts/ci/verify_workflow_contracts.sh` | PASS | PASS |
| `bash scripts/ci/verify_production_postman_parity.sh` | PASS | PASS |
| `python tools/check_postman_artifacts.py` | PASS | PASS |
| `python tools/check_markdown_links.py` | PASS | PASS |
| `make postman-check` | Not run (make unavailable) | Not run |
| `make api-contract-check` | Not run (make unavailable) | Not run |

## 9. Test results

All runnable gates **PASS** before and after cleanup. No cleanup-caused failures.

## 10. Failures and reasons

| Item | Reason |
|------|--------|
| `make postman-check` / `make api-contract-check` | GNU `make` not on Git Bash PATH (Windows host); CI script equivalents used |
| Production Newman live run | Skipped by design (`NEWMAN_SKIP`; requires prod credentials) |

No pre-existing test failures.

## 11. Safety confirmations

- No production deploy was run
- No production DB mutation was run
- No migrations were deleted (15 tracked files unchanged)
- No sqlc or protobuf generated code was deleted
- No GitHub workflows were deleted (20 tracked files unchanged)
- No Postman CI assets were deleted
- No e2e fixtures under `tests/e2e/data/` were deleted
- No secrets were introduced

## Related reports

- [Baseline](2026-07-02-tmp-artifact-cleanup-baseline.md)
- [Inventory](2026-07-02-tmp-artifact-cleanup-inventory.md)
- Dry-run list: `.scratch/cleanup-candidates.txt`

## Prerequisite

Deep-clean committed as `73a06c3b` on `cleanup/repo-deep-clean-docs-reports-scripts` before this branch was created.
