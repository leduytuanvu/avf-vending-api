// Package rbac is a documentation anchor for interactive admin RBAC.
//
// Fine-grained permission strings and role→permission expansion live in internal/platform/auth:
// PermissionsForRole, HasPermission, RequirePermission / RequireAnyPermission middleware, and JWT roles from accounts.
package rbac
