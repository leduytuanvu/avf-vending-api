// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: critical_telemetry.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const GetCriticalTelemetryEventStatus = `-- name: GetCriticalTelemetryEventStatus :one
SELECT
    machine_id,
    idempotency_key,
    status,
    event_type,
    accepted_at,
    processed_at,
    updated_at
FROM
    critical_telemetry_event_status
WHERE
    machine_id = $1
    AND idempotency_key = $2
`

type GetCriticalTelemetryEventStatusParams struct {
	MachineID      uuid.UUID
	IdempotencyKey string
}

func (q *Queries) GetCriticalTelemetryEventStatus(ctx context.Context, arg GetCriticalTelemetryEventStatusParams) (CriticalTelemetryEventStatus, error) {
	row := q.db.QueryRow(ctx, GetCriticalTelemetryEventStatus, arg.MachineID, arg.IdempotencyKey)
	var i CriticalTelemetryEventStatus
	err := row.Scan(
		&i.MachineID,
		&i.IdempotencyKey,
		&i.Status,
		&i.EventType,
		&i.AcceptedAt,
		&i.ProcessedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const UpsertCriticalTelemetryEventStatus = `-- name: UpsertCriticalTelemetryEventStatus :exec
INSERT INTO critical_telemetry_event_status (
    machine_id,
    idempotency_key,
    status,
    event_type,
    accepted_at,
    processed_at
)
VALUES ($1, $2, $3, $4, $5, $6)
ON CONFLICT (machine_id, idempotency_key) DO UPDATE
SET
    status = EXCLUDED.status,
    event_type = COALESCE(critical_telemetry_event_status.event_type, EXCLUDED.event_type),
    accepted_at = COALESCE(critical_telemetry_event_status.accepted_at, EXCLUDED.accepted_at),
    processed_at = COALESCE(EXCLUDED.processed_at, critical_telemetry_event_status.processed_at),
    updated_at = now()
`

type UpsertCriticalTelemetryEventStatusParams struct {
	MachineID      uuid.UUID
	IdempotencyKey string
	Status         string
	EventType      pgtype.Text
	AcceptedAt     pgtype.Timestamptz
	ProcessedAt    pgtype.Timestamptz
}

func (q *Queries) UpsertCriticalTelemetryEventStatus(ctx context.Context, arg UpsertCriticalTelemetryEventStatusParams) error {
	_, err := q.db.Exec(ctx, UpsertCriticalTelemetryEventStatus,
		arg.MachineID,
		arg.IdempotencyKey,
		arg.Status,
		arg.EventType,
		arg.AcceptedAt,
		arg.ProcessedAt,
	)
	return err
}
