# Generates Go under ../gen/go when run (optional; gen output may be gitignored in some setups).
version: v2
plugins:
  - remote: buf.build/protocolbuffers/go
    out: ../gen/go
    opt:
      - paths=source_relative
